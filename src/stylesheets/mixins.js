const DRUK_TYPE = `
  font-family: 'Druk', sans-serif;
  font-size: 1em;
  line-height: 1.2em;
  font-style: normal;
  font-weight: 500;
`;

const DRUK_TEXT_TYPE = `
  font-family: 'Druk Text', sans-serif;
  font-size: 1em;
  line-height: 1.2em;
  font-style: normal;
  font-weight: 500;
`;

const ROBOTO_TYPE = `
  font-family: 'Roboto', sans-serif;
  font-size: 1em;
  line-height: 1.6em;
  font-style: normal;
  font-weight: 400;
`;


const ROBOTO_ITALIC_TYPE = `
  font-family: 'Roboto', sans-serif;
  font-size: 1em;
  line-height: 1.6em;
  font-style: italic;
  font-weight: 400;
`;

const ROBOTO_BOLD_TYPE = `
  font-family: 'Roboto', sans-serif;
  font-size: 1em;
  line-height: 1.6em;
  font-style: normal;
  font-weight: 700;
`;


const ABSOLUTE_MIDDLE = `
  transform: translate(0, -50%);
`;

const ABSOLUTE_CENTER = `
  transform: translate(-50%, 0);
`;

const ABSOLUTE_CENTER_MIDDLE = `
  transform: translate(-50%, -50%);
`;

export default {
  DRUK_TYPE,
  DRUK_TEXT_TYPE,
  ROBOTO_TYPE,
  ROBOTO_BOLD_TYPE,
  ROBOTO_ITALIC_TYPE,
  ABSOLUTE_CENTER,
  ABSOLUTE_CENTER_MIDDLE,
  ABSOLUTE_MIDDLE
};